
// Este arquivo agora serve apenas como referência de tipos.
// A lógica de execução SQL foi movida para o storage.ts para uso direto no frontend.
export interface SQLiteCloudConfig {
  connectionString: string;
  apiKey: string;
}
